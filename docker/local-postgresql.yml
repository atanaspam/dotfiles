services:
  db:
    image: postgres:18
    restart: always
    shm_size: 256mb
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - "~/tmp/pgdata:/var/lib/postgresql/data/pgdata"
    ports:
      - "5432:5432"
